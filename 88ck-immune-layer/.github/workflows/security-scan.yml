name: Legacy Placeholder Security

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow moved
        run: echo "Moved to repo root .github/workflows/security-scan.yml"
