apiVersion: v1
kind: Service
metadata:
  name: 88ck-gateway
spec:
  type: {{ .Values.service.type }}
  selector:
    app.kubernetes.io/name: 88ck-gateway
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
