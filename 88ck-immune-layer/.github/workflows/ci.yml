name: Legacy Placeholder CI

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow moved
        run: echo "Moved to repo root .github/workflows/ci.yml"
